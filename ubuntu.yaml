# Ubuntu config file.

- defaults:
    link:
        create: true
        relink: true
        force: true

- clean: ["~", "~/.config"]

- link:
    ~/.bashrc: shell/bashrc
    ~/.dotfiles: ""
    ~/.gitconfig: tools/gitconfig
    ~/.gitignore_global: tools/gitignore_global

