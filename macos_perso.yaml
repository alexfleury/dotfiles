# Macos (personal) config file.

- defaults:
  link:
    relink: true

- clean: ["~"", "~/.config"]

- link:
  ~/.dotfiles: ""
  ~/.gitconfig: tools/gitconfig
  ~/.gitignore_global: tools/gitignore_global
  ~/.zshrc: shell/zshrc