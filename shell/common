# Shell useful aliases.

# Custom scripts.
export PATH="$HOME/.dotfiles/bin:$PATH"

# Aliases for sudo.
alias sudo="sudo "

# Default editor.
export EDITOR=vim

# Change directory aliases.
alias cd..="cd .."
alias ..="cd .."

