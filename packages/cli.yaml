# Packages to install for the CLI environment.

- omnipkg:
    - update

    - install: [
        bat,
        fastfetch,
        fzf,
        neovim,
        wget,
        zoxide,
        {linux: zsh, require_gui: true}
    ]

