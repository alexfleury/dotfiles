# Packages to install for the CLI environment.

- omnipkg:
    - update

    - install: [
        bat,
        fastfetch,
        fzf,
        neovim,
        pyenv,
        pyenv-virtualenv,
        wget,
        zoxide,
        [ttf-fira-code, fonts-firacode, font-fira-code],
        {linux: zsh}
    ]

